/**
 * Settings screen (fase 6): ganti proxy URL, set/clear cookie YouTube,
 * feedback status koneksi. Slot navigasi aktif; implementasi menyusul.
 */
import { View, StyleSheet } from 'react-native';

export function SettingsScreen() {
  return <View style={styles.placeholder} />;
}

const styles = StyleSheet.create({
  placeholder: { flex: 1 },
});
