/**
 * Library screen (fase 6): liked songs & playlist akun (cookie).
 * Slot navigasi aktif; implementasi UI menyusul setelah playback PoC.
 */
import { View, StyleSheet } from 'react-native';

export function LibraryScreen() {
  return <View style={styles.placeholder} />;
}

const styles = StyleSheet.create({
  placeholder: { flex: 1 },
});
